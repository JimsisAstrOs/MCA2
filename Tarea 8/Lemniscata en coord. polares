import numpy as np
import matplotlib.pyplot as plt

a = 2

theta = np.linspace(0, 2*np.pi, 1000)

r = a*(1 + np.cos(theta))

x = r*np.cos(theta)
y = r*np.sin(theta)

plt.plot(x,y)
plt.axis('equal')
plt.grid()
plt.title("Cardioide")
plt.show()
